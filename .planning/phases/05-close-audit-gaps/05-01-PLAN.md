---
phase: 05-close-audit-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lauos/src/app/(protected)/finance/finance-sub-nav.tsx
autonomous: true
requirements:
  - CATG-01
  - CATG-02

must_haves:
  truths:
    - "A 'Categorías' tab is visible in the finance sub-nav on all /finance/* pages"
    - "Clicking the Categorías tab navigates to /finance/categories"
    - "The Categorías tab shows the active accent-color bottom border when on /finance/categories"
    - "The existing Cuentas, Insights, and Transacciones tabs continue to work correctly"
  artifacts:
    - path: "lauos/src/app/(protected)/finance/finance-sub-nav.tsx"
      provides: "FinanceSubNav with 4-link FINANCE_NAV_LINKS array"
      contains: "'/finance/categories'"
  key_links:
    - from: "finance-sub-nav.tsx FINANCE_NAV_LINKS"
      to: "/finance/categories route"
      via: "Link href='/finance/categories'"
      pattern: "href.*finance/categories"
---

<objective>
Add a Categorías link to the FinanceSubNav component so the categories CRUD page is reachable via normal navigation.

Purpose: CATG-01 and CATG-02 are audit blockers. The /finance/categories page is fully implemented (verified in 03-VERIFICATION.md, 12/12 passed) but has no navigation entry — users can only reach it by typing the URL directly. This single-object addition to FINANCE_NAV_LINKS closes both gaps.

Output: finance-sub-nav.tsx with 4 links (Cuentas | Insights | Transacciones | Categorías). No other files change.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-close-audit-gaps/05-RESEARCH.md
</context>

<interfaces>
<!-- Current state of finance-sub-nav.tsx (the only file being modified) -->

From lauos/src/app/(protected)/finance/finance-sub-nav.tsx:
```typescript
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { cn } from '@/lib/utils'

const FINANCE_NAV_LINKS = [
  { href: '/finance/accounts', label: 'Cuentas' },
  { href: '/finance/insights', label: 'Insights' },
  { href: '/finance/transactions', label: 'Transacciones' },
]

export function FinanceSubNav() {
  const pathname = usePathname()

  return (
    <nav
      className="flex items-center gap-1 border-b border-border px-4 pt-2"
      aria-label="finance sub-navigation"
    >
      {FINANCE_NAV_LINKS.map((link) => {
        const isActive = pathname.startsWith(link.href)
        return (
          <Link
            key={link.href}
            href={link.href}
            className={cn(
              'px-3 py-2 text-sm font-medium transition-colors duration-150 border-b-2 -mb-px',
              isActive
                ? 'border-[var(--color-accent)] text-foreground'
                : 'border-transparent text-muted-foreground hover:text-foreground hover:border-border'
            )}
          >
            {link.label}
          </Link>
        )
      })}
    </nav>
  )
}
```

Active-state logic uses `pathname.startsWith(link.href)`. No /finance/categories sub-routes exist (only categories/page.tsx and categories/categories-client.tsx), so startsWith is safe.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Categorías link to FINANCE_NAV_LINKS</name>
  <files>lauos/src/app/(protected)/finance/finance-sub-nav.tsx</files>
  <action>
    Add one object to the end of the FINANCE_NAV_LINKS array:

    ```typescript
    { href: '/finance/categories', label: 'Categorías' },
    ```

    The final array must be:
    ```typescript
    const FINANCE_NAV_LINKS = [
      { href: '/finance/accounts', label: 'Cuentas' },
      { href: '/finance/insights', label: 'Insights' },
      { href: '/finance/transactions', label: 'Transacciones' },
      { href: '/finance/categories', label: 'Categorías' },
    ]
    ```

    No other changes to this file. No other files need modification — the existing render loop, active-state logic (pathname.startsWith), and CSS classes handle the new entry automatically.

    Placing Categorías last (position 4) is intentional: categories is a management/config page, not a primary view. Users visit it less frequently than transactions or insights. This matches the convention of settings/config appearing at the end of navigation.

    After editing, run the TypeScript build to verify zero errors:
    `cd /Users/laucaballero/Desktop/Lautaro/AndesCode/lauOS/lauos && PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH" npm run build 2>&1 | tail -5`

    Then run the shell spec to confirm navigation tests still pass:
    `cd /Users/laucaballero/Desktop/Lautaro/AndesCode/lauOS/lauos && npx playwright test tests/shell.spec.ts --reporter=line`
  </action>
  <verify>
    <automated>cd /Users/laucaballero/Desktop/Lautaro/AndesCode/lauOS/lauos && PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH" npm run build 2>&1 | grep -E "error|Compiled|Failed"</automated>
  </verify>
  <done>
    - finance-sub-nav.tsx FINANCE_NAV_LINKS array contains exactly 4 entries with /finance/categories as the last href
    - TypeScript build exits 0 with no errors
    - The string 'Categorías' and '/finance/categories' both appear in the file
  </done>
</task>

</tasks>

<verification>
After the task commit:

1. Verify the file change: `grep -n "categories" /Users/laucaballero/Desktop/Lautaro/AndesCode/lauOS/lauos/src/app/(protected)/finance/finance-sub-nav.tsx` — must return a line with `/finance/categories` and `Categorías`

2. Verify build is clean: `cd /Users/laucaballero/Desktop/Lautaro/AndesCode/lauOS/lauos && PATH="/Users/laucaballero/.nvm/versions/node/v20.19.6/bin:$PATH" npm run build` — must exit 0

3. Verify existing nav links still present: `grep -c "href.*finance" /Users/laucaballero/Desktop/Lautaro/AndesCode/lauOS/lauos/src/app/(protected)/finance/finance-sub-nav.tsx` — must return 4
</verification>

<success_criteria>
- finance-sub-nav.tsx contains `{ href: '/finance/categories', label: 'Categorías' }` as the 4th entry in FINANCE_NAV_LINKS
- TypeScript build passes with zero errors
- No other files were modified
- CATG-01 and CATG-02 are navigable requirements: /finance/categories reachable via the tab
</success_criteria>

<output>
After completion, create `.planning/phases/05-close-audit-gaps/05-01-SUMMARY.md`
</output>
